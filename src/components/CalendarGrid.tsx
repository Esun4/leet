import type { Problem } from '../types/index';

type CalendarGridProps = {
    problemsByDate: Record<string, Problem[]>;

};

export default function CalendarGrid({ problemsByDate }: CalendarGridProps) {
    return;
}